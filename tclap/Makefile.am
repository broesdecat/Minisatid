
ACLOCAL_AMFLAGS = -I config

SUBDIRS = include config # examples docs tests msc

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = $(PACKAGE).pc
EXTRA_DIST = $(PACKAGE).pc.in

DISTCLEANFILES = $(PACKAGE).pc
